using System;

namespace Deveel.Math {
	class ExpCalculator {
		public static readonly ExpCalculator Instance = new ExpCalculator();

		public BigDecimal Calculate(BigDecimal value, MathContext mc) {
			throw new NotImplementedException();
		}
	}
}